/**
 * title component for the app
 * @returns JSX element of the title
 */
function Title() {
  const title = "Project Management App";
  return <h1 className="text-white mt-3 text-center font-serif">{title}</h1>;
}

export default Title;
